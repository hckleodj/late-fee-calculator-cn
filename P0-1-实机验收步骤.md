# 大进车贷助手 P0-1 实机验收步骤

只使用测试地址和模拟客户。不要在正式地址操作，不要导入唯一一份真实客户数据。

## 开始前

1. 在微信中打开普通测试地址，确认没有 P0-1 调试区。
2. 再打开调试地址（测试地址末尾增加 `?backupDebug=1`）。
3. 记录初始 `dataRevision`、`dirtySinceBackup`、`lastBackupAt` 和当前数据 hash。

## 场景 1：首次打开

不做任何修改，查看方案测算、还款提醒和客户管理页面。刷新一次，确认页面无报错，`dataRevision` 没有变化。

## 场景 2：新增客户

新增“P0-1测试客户”，使用模拟车牌和模拟金额。保存后确认客户立即出现，`dataRevision +1`、`dirtySinceBackup=true`。刷新后确认客户和状态仍存在。

## 场景 3：编辑客户

给测试客户备注增加“编辑测试”。保存后确认 `dataRevision +1`、dirty 仍为 true。刷新后确认备注存在。

## 场景 4：登记收款

给测试客户登记一笔模拟收款。确认收款记录和还款余额正常，`dataRevision +1`、dirty 为 true。刷新后确认收款记录仍存在。

## 场景 5：纯计算

依次执行方案测算、月供倒推和滞纳金计算。记录操作前后状态，确认 revision、dirty、客户主数据 hash 都不变化。

## 场景 6：复制备份

进入“客户管理 → 数据备份”，点击“复制备份文本”。确认生成内容正常，但 dirty 不变成 false，`lastBackupAt` 不更新。

## 场景 7：分享或导出

点击 JSON 导出并完成或取消系统分享流程。确认页面不显示“系统已确认备份完成”，dirty 仍为 true，`lastBackupAt` 不更新。

## 场景 8：关闭和重新进入

关闭网页后，从微信重新打开同一个测试地址。确认测试客户、备注、收款记录和 revision 都保留，重新打开不会增加 revision。

## 记录结果

分别记录微信 iOS 和 Android：页面打开、新增、编辑、收款、刷新、重新进入、复制备份是否正常。没有对应实机时明确写“待验收”。
