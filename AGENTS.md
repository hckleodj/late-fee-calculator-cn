# 工程规则

1. 新开发或接手任务前，必须先阅读 `docs/PROJECT_HANDOFF.md`。
2. 未经用户明确授权，不得直接部署生产或修改正式页面。
3. 数据、备份、迁移和金融公式相关改动，必须先完成技术预检并取得明确结论。
4. localStorage、数据结构、备份/恢复、删除、金融公式及其他高风险修改，必须先在隔离环境开发和测试。
5. 正式主数据 key `lateFeePaymentPlansV1` 及其他正式存储 key 不得擅自改名、迁移或替换。
6. 不得自动初始化、覆盖、清空或删除正式客户数据；异常时必须进入安全保护。
7. 下载、复制、分享或其他 API 返回成功，不得被认定为备份已经真实落盘；必须保留人工确认与 revision/hash 复核。
8. 发布过程中禁止顺手重构、调整公式或加入未经自动化测试和真机验收的新功能。
9. 每次正式发布前必须创建并验证回滚点，记录基线commit、diff和线上文件状态。
10. 移动端关键能力以 OPPO N6 真机结果为最终依据，不得只依赖桌面模拟器或API规范。
